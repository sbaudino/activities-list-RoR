%ul
  %li
    =link_to list.name, list_tasks_path(list), remote: true
    =link_to "Editar", edit_list_path(list)
    =link_to "Eliminar", list_path(list), method: :delete, data: { confirm: 'Are you sure?' }
    =link_to "Agregar Tarea", new_list_task_path(list)